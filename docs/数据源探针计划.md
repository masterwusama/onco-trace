# 数据源探针计划（P0）

这份文档管一件事：在建任何业务表之前，先实测每个维度到底有没有能自动落库的公开源、覆盖率多少。
产出物是 `source_probe_log` 表 + 一张 18 病 × 8 维的覆盖度矩阵，由它裁定 MVP 建哪些表。

数据全部来自公开源抓取，仓库里没有人工录入模块。人工判断只以两种形式存在：
仓库内的声明式配置（`etl/onco_etl/targets.py`、`sources.py`），以及对抽取草稿的一次目测抽查。

## 出口判据

判据是"建表之前必须实测到的东西"。实测列填的是探针跑出来的数，未跑的批次留空。

| 维 | 判据 | 实测（2026-09-07） | 不达标的处置 |
|---|---|---|---|
| 器官树 | ICD-O-3 拓扑码 ≥150 行可解析；18 病 100% 落到具体节点 | ✅ 82 个 site recode / 332 个拓扑码 / 806 个形态学码，18/18 挂载到器官级分组 | 换 ICD-10 章节自建两级树，放弃亚部位 |
| ID 主干 | 有一个跨源 ID 枢纽对 18 病全覆盖，且由它带出 MeSH / EFO / ICD-O-3 | ✅ 枢纽定为 NCIt（MONDO 主条目的 NCIT xref 18/18）。⚠️ MONDO 自身带不动：UMLS 17/18、MESH 7/18、EFO 5/18 | 退化为 ICD-10 单键，接受跨源关联缺口 |
| 统计层 | 每病 ≥1 个 metric × ≥5 年 × ≥10 年龄组 × 性别覆盖与病种声明一致 | ✅ 发病达标（有条件）：SEER 18/18 页、每页 4 个 metric、年度 ≥5 年，但年龄组只有 8 档宽分组；GBD 已排除（词表有 1 岁一档，118 个数值文件全在登录门后）；年龄组那一半由 GCO Cancer Over Time 补上——中国 18 个 5 岁档 × 16 个年度（2002–2017）× 三性别，代价是只有发病没有死亡，且是 5 个登记处覆盖 60% 人口的外推而非全国序列；备选 WHO GHO 也已排除（AGEGROUP 与 GHECAUSES 两维齐，但带两维的指标零中国行、有中国行的无年龄维，0/18） | 发病维进 MVP；死亡年龄组只展示美国口径，中国那一半只剩 IHME 注册账号一条路（B7c 已把入口实测定位：GBD Results 页一打开就自动弹注册对话框，顶栏 Account → Register 是同一件事的第二条路，注册与登录同一条 B2C 流，见 docs/MVP裁定.md），账号到位之前该模块留空 |
| 生存率 | 每病 5 年生存率 ≥3 个分期档 | ✅ SEER 17/18 页 ≥3 档。⚠️ 白血病整页无分期表（源本身不提供，非抓取失败）；⚠️ 观测窗只到 2018，2019–2023 是拟合值 | 只展示全分期一个数，并在页面标口径 |
| 危险度 | 每病 ≥3 个危险因素带 PAF 或 OR + CI | ⚠️ B4 已跑 GBD CRA 这一半：**效应量 0/18**（vizhub 数据面四个路由全 401，只有 `/api/config` 匿名，回的是版本号）；关联骨架可达——A2 交叉表匿名给出 2390 个 cause×REI 对，剔掉聚合档后 **10/18** 病有 ≥3 个独立危险因素（brain 一档都没有）。GWAS Catalog 那一半相反：**效应量有、暴露语义没有**——匿名整包 1,192,032 行里 `OR or BETA` 非空 84.3%、`95% CI` 含可解析区间 75.0%，但全表没有 PAF 列，而且那是遗传位点不是可干预暴露；按 targets 主条目 URI 判 **14/18**，P1 的 C1a 把掉队四病裁成 `GWAS_URI` 声明档后按「主条目 + 声明档」判 **18/18** | 关联与 role 照常落库，`paf` 两个源都给不了：CRA 供可干预暴露清单但不带强度，GWAS 只作为 `role='genetic'` 一层带 OR/β 与区间。页面按"有证据关联、无归因强度"标，不做数值榜 |
| 前沿研究 | 每病在注册试验 >0、近 5 年文献 >0、OpenTargets 关联 ≥10 | ✅ B5 三列全过 18/18：在招试验合计 23,660 项（最少 thyroid 292），近 5 年文献合计 765,891 篇且每病都有 OA 命中（最少 uterus 10,366 篇，OA 占 50.8%），OT 关联靶点最少 brain 3,868 个（判据 10；换档前最少是 breast_female 643）。⚠️ 疾病键只能逐病声明查询词——CT 无可对齐的 MeSH ID、EPMC 的 `MH:` 只覆盖全库 2.2%；⚠️ CT 匿名侧没有地理过滤器，"中国参与"只能落地点数组再前端筛；⚠️ 窄档问题 B7c 已裁：18 病里**只有乳腺癌**改查宽节点 `MONDO:0007254`（breast cancer，换后 assoc 17,064 / lit 724,160 / drugs 1,036，换前 643 / 88 / 0），胰腺保持声明节点 `MONDO:0009831`——它的父节点 pancreatic neoplasm 在研药只有 30 而本节点 463，换过去研究层反而更空。覆盖写成 `targets.OT_NODE` 一份声明，探针只读它、不自己按名字换档 | 该维从 MVP 剔除 |
| 症状 | ≥12/18 病抽出 ≥4 条真症状，5 病抽查 precision ≥80% | ✅ B6 过判据：PDQ 病人版症状小节 L2 规则解析 **18/18** 病各 ≥4 条、合计 209 条，5 病目测 72/72 真症状（precision 100%，线 ≥80%）。⚠️ `freq_band` 无源——症状小节百分号出现数实测 0（hp 版整页 1,857 个百分号全是生存率与缓解率），全站只有 Orphanet 六档能给而它常见上皮癌 0 命中；⚠️ **中文症状名 11/18 空缺**——PDQ 只有英文，现成中文清单是 WHO 中文版 3 病（禁商用）∪ 中文维基 5 病 ＝ **7/18**。B6c 四条匿名公开路由 `probes/wikidata.py` 实测（`source_probe_log` id 130）都不成立：Wikidata 症状子树词典 precision **37.5%**（168 行目测 对 63 / 泛 43 / 错 62，判据线 80%，错例 `weight loss → 减肥`、`loss of appetite → 耳咽管開放症`）、拿 PDQ 原文逐条查的天花板只有 **21.3%**（169 个去重写法里 ≤2 词仅 36 个）、中文维基 **5/18** 病有 ≥4 条真症状清单而 11 病写成散文 | 症状维进 MVP；不达线的病显示"暂无可靠来源"（实测 0 病如此，18/18 全过）。频率带这一列按源留空、UI 不画成定量结论；**中文名列不做成翻译列**，`symptom` 按 `(source_id, disease_code, name, name_lang)` 落，PDQ 行 `name_lang='en'` |

## 为什么 P0 不建业务表

`db/schema.sql` 只有 `source`、`dataset_release`、`source_probe_log`、`etl_job_log` 四张表。
症状维已经验证过一次"文档说有、实测没有"：HPO、Orphanet、NCIt 都声称提供疾病↔症状注释，
实测常见上皮癌拿不到可用行（证据见下节）。先按想象建 20 张表，探针回来发现喂不进数据，
就要再写一轮迁移改掉。覆盖度矩阵出来后再建表，一次建对。

这条纪律已经走完：B1–B7c 全 ✅，出口判据的两个产出（自动生成的 `docs/数据源覆盖度.md`
与逐维裁定的 `docs/MVP裁定.md`）都在库里，业务表从 P1 起建，建哪几张以 `docs/MVP裁定.md` 那张表为准。

## B2 实测结论：ID 主干与器官树

两维都过判据，但 ID 主干的枢纽换了源。

**器官树**——SEER 的 `sitetype.icdo3.d20220429.xlsx`（451875 字节 / 12496 行）一份文件同时供
anatomy 与 histology 两维：82 个 site recode（器官级分组）、332 个拓扑码、806 个去重形态学码。
18 病按 ICD-O-3 拓扑码段展开后与 site recode 求交，18/18 命中。
亚部位下钻（器官 → 具体部位）不用 NCIt 的 237 MiB 全量，改用 MONDO 里 145 个带 ICD-9 码的
亚部位 term，例如 `bronchus cancer`、`anal canal cancer`，粒度正好是页面要的那一层。

**ID 主干**——MONDO `releases/2026-09-01`（.obo 53134854 字节 / 63278 个 term）。
18 个疾病主条目的 MONDO ID 声明在 `etl/onco_etl/targets.py`，探针只做校验：
存在、非 obsolete、带齐必需的 xref。当前 18/18 通过。

主条目的 xref 完整度是这一批最重要的产出，它直接决定了谁是跨源枢纽：

| xref 前缀 | 18 病覆盖 | 缺口 | 用途 |
|---|---|---|---|
| NCIT | 18/18 | — | 跨源枢纽 |
| UMLS | 17/18 | uterus | 临床术语兜底 |
| MESH | 7/18 | 11 个常见上皮癌 | Europe PMC 文献检索的查准率 |
| EFO | 5/18 | 13 个 | Open Targets 对齐（GWAS 不受此限：它的 trait 词表本身以 MONDO 为键，见 B4） |

MONDO 带不动 MeSH 与 EFO，所以枢纽落在 NCIt 上（NCIt 自带 MeSH / ICD-O-3 / ICD-10 / EFO
交叉引用，一次补齐三个缺口），文献维另引 NLM 的 MeSH 描述符全量。两个源都已登记：

| code | 维 | 实测入口 | 体量 | 可达性 |
|---|---|---|---|---|
| `ncit` | identity, anatomy | `purl.obolibrary.org/obo/ncit.obo` → GitHub release asset | 248162694 字节 | 直连与代理都取到过（2026-09-08 这趟是 proxy） |
| `mesh` | identity, literature | `nlmpubs.nlm.nih.gov/projects/mesh/MESH_FILES/xmlmesh/desc2026.xml` | 312952703 字节 | direct |

两个都是年度/季度下一次的大文件，`upstream_version` 一律从文件头的 `data-version` 读，
不从 URL 读——NCIt 的 URL 里写的是 `latest`，MeSH 的 URL 里写的是年份，都会随发布漂移。

**症状与定位维**——MONDO 的 `disease_has_location`→UBERON 全库只有 775 行、
`disease_has_feature`→HP 只有 819 行，摊到 63278 个 term 上约 1%。这与下节症状维的结论一致：
结构化本体喂不出症状维。但兜底不是 L3——B6a 实测正文 HTML 的症状清单是现成 `<li>`，
L2 规则解析就过判据（见《B6 实测结论》）。定位维同理不靠 MONDO 这条稀疏关系，
靠的是 SEER site recode（上节，82 分组 / 332 拓扑码，18/18 挂载）。

复现（离线重放已归档的文件，不重新下载）：

```powershell
ops\etl.ps1 probe --code icdo3_seer --code mondo --offline
ops\etl.ps1 probe-status
```

## B3 实测结论：统计层与生存率

四个源都跑完，各给一半：SEER 给了数但年龄粒度只有 8 档宽组，GBD 粒度够但数值全在登录门后，
GCO 门户下的两个数据库合起来正好补上缺口——Cancer Today 有中国国家级单点估计但没有年龄维，
Cancer Over Time 有 16 年 × 18 个 5 岁档但中国只有发病没有死亡。最后一条备选 WHO GHO 已排除：
年龄组与死因两维在维表里都齐，但带这两维的指标没有中国国家级行，有中国行的又都没有年龄维。

### SEER Cancer Stat Facts：达标，但年龄组不达标

`seer.cancer.gov/statfacts/html/<slug>.html`，18 病一页，共 1546896 字节，
`upstream_version` 取 18 页 `Last-Modified` 的最大值 `2026-04-22`（SEER 不给版本号，
statfacts 全站也没有发布日字段）。18 个 slug 与 target 的对应关系声明在探针里
（`lung→lungb`、`colorectum→colorect`、`uterus→corp` 等），逐个用 `<h1>` 标题核过语义。

**抓取契约**：每页 8 张 `class="scrapeTable"` 的表，但只有三种形状，且 `id="scrapeTable_NN"`
的编号与表在文档里的出现顺序不一致（实测 `lungb` 是 `_01 _02 _04 _05 _03 _07 _08 _06`）。
所以认领表格只能按文档顺序走，用最近的 `<strong>` 标题判定这张表填哪一维。
年度序列表是两行表头——第一行 4 个 metric 各 `colspan=2`，第二行 `Observed / Modeled Trend`；
种族费率表则根本没有 `<thead>`，6 行全是数据。按"第一行是表头"的通用假设切会两头错。

**四个 metric 的观测窗互不相同，这是这一批最该带走的数**：

| 年度序列表列 | 观测年数 | 区间 |
|---|---|---|
| Death Rate — U.S. / Observed | 50 | 1975–2024 |
| Rate of New Cases — SEER 8 / Observed | 49 | 1975–2023 |
| Rate of New Cases — SEER 12 / Observed | 32 | 1992–2023 |
| 5-Year Relative Survival — SEER 8 / Observed | 44 | **1975–2018** |
| 5-Year Relative Survival — SEER 8 / Modeled Trend | 49 | 1975–2023 |

18 页的 min 与 max 完全相等，说明这不是个别病缺数据，而是全站口径：**五年存活率的观测值停在一期，
2019–2023 只有模型趋势线**。所以 `survival` 必须把 Observed 与 Modeled Trend 分开存（`is_observed` 那列），
前端画生存率曲线要标出哪一段是拟合——否则等于把外推当观测发布。

**判据改过一处**：第一版写的是"年龄分布双性别齐备"，18 页里判掉 5 页，而掉的恰好是乳腺、宫颈、
卵巢、子宫体、前列腺——性别特异癌在 SEER 只出一张表、页面上没有 `<h5>` 性别标签，性别只写在
口径行里（`All Races, Females`）。那是判据错不是数据缺。改为按 `targets.py` 新增的 `sex` 列校验，
并且口径行里出现对立性别时报 `conflict`（本仓库声明写错）而不是 `missing`（源缺），两者混报会引着人去查错的那一边。

**白血病无分期表**：`leuks.html` 全文没有 "5-Year Relative Survival by Stage at Diagnosis"，
只有 7 张 scrapeTable，生存率是一条 1975–2018 的时间序列。血液肿瘤本无实体瘤分期，
所以按页豁免而不是放宽判据——NHL 与骨髓瘤同为血液肿瘤却各有 Ann Arbor 分期表（5 档、4 档），
不能按 `category='heme'` 整类豁免。豁免名单是反着盯的：上游哪天补了表，探针会点名要求把它捡回来。
落地的处置即出口判据里那一行：白血病页展示全分期一个数并标口径。

**年龄组 8 档，未达 ≥10**：`<20, 20–34, 35–44, 45–54, 55–64, 65–74, 75–84, >84`。
画发病/死亡年龄分布够用，但做不了 5 岁组标化率。SEER*Explorer 底层是 2000 美国标准人口的
20 个 5 岁组，要更细得走它内部的 `render_region_*.php` JSON 接口——无公开文档、表单参数需逆向。
这一档缺口原本留给 GBD，GBD 实测之后不成立了（见下一节）。GCO 那两条匿名入口（再下一节）
已经把它补上：Cancer Over Time 给到 18 个 5 岁档 × 16 个年度，所以 5 岁组标化率这一项展示
保住，代价是它在中国是登记处抽样口径（5 个登记处、覆盖 60% 人口、只到 2017 年、只有发病没有
死亡），必须与 SEER 美国口径并排标而不是混算。WHO GHO 那条备选已跑完并排除（见其下小节，
实测 0/18），中国死亡年龄组这一半仍空，只剩 IHME 注册账号一条路。B7c 已裁：这一维不进 MVP，
页面按"暂无可靠来源"显示，账号到位后重测（注册入口实测定位见《B7c 收口》）。
本批只上报不卡判据。

顺带量到种族/民族分层可用：6 组（All Races、Hispanic、Non-Hispanic AI/AN、API、Black、White），
发病与死亡各一套双性别表，MVP 不展示但落库时不必丢掉。

**未了的一件事**：`sources.py` 里给这个源写的 `legal_note` 要求"HTML 表结构改版频繁，
解析器必须留 fixture 回归"，但 `data/raw/` 整个在 `.gitignore` 里，18 页归档只存在于本机。
现在换一台机器 clone 下来，`--offline` 直接报"需要先有一份归档"，解析器没有任何回归保护。
不能一刀切要求所有源都检出归档——MONDO 那份 .obo 有 51 MB。所以这一条对 SEER（1.5 MB，
且三种表格形状里有两种是判据分支的边界：`leuks` 无分期表、`prost` 无性别标签）先欠着，
要么检 2~3 页代表页进仓库，要么在 P1 建表前把 fixture 约定建起来（B7c 没动这笔，已记在
`docs/MVP裁定.md`《这份裁定还没有覆盖的事》）。

复现：

```powershell
ops\etl.ps1 probe --code seer_statfacts --offline
ops\etl.ps1 probe-status
```

### GBD：设计口径全中，数值一条都不给

这一维的结论不是"GBD 能不能用"，而是"统计层缺一角还是缺整角"。所以判据拆两半分别裁定，
`verdict=blocked`、`diseases_covered=0`。

**词表这一半匿名可取**——`/sites/default/files/ihme_query_tool/IHME_GBD_2021_CODEBOOK.zip`，
127869 字节，里面一份横向词表 CSV 加四张层级 XLSX。注意版本是 2021：Results Tool 界面上的
Codebook 按钮到今天仍指向它，2023 版的词表跟着各 record 一起进了登录门。

| 词表维度 | 实测 | 判据 |
|---|---|---|
| 病因 | 18/18 病各有 L3 档，其下另带 13 个 L4 亚档：肝癌 6 档（肝炎 B/C、酒精、NASH、其他，加儿童肝母细胞瘤）、白血病 5 档按类型、NHL 2 档 | — |
| 地点 | 818 个，`China` = location_id 6（Level 3） | 中国 ✅ |
| 年度 | 1990–2021，32 个值 | ≥5 年 ✅ |
| 性别 | Male / Female / Both | 双性别 ✅ |
| 年龄组 | 155 档，其中 1 岁一档 94 个 | ≥10 档 ✅ |
| 度量 | Deaths / Incidence / Prevalence / YLDs 四个都在（词表共 21 个度量） | ✅ |

SEER 差的那一角，GBD 的设计里全有。

**数值这一半三条路都堵着**：

1. GHDX 的 22 个 GBD 2023 record 页面上，118 个文件行的 `<a href>` 一律被换成
   `/download-access/login`，而文件名、字节数、DOI、发布日照旧写在页面上（合计 6.3 GiB，
   最大单项 206 MB）。
2. 其中 2 个 record 整页要密码：**返回 HTTP 200 的 Protected Page**。这是本批最容易写错的一处——
   按状态码判可用，就会把它记成"页面正常、只是没有文件"，一个授权问题被写成源缺数据。
   认 `<title>` 才认得出来，离线重放时也只有标题可认。
3. Results Tool 自己的查询接口要 Azure AD B2C 换来的 token（`login.healthdata.org`，
   scope `https://ihmecsu.onmicrosoft.com/data-api/data.read`），打进前端的还是本地开发默认值
   `http://127.0.0.1:5000/protected-api`；界面前另有一层 Cloudflare 机器人校验。

**为什么词表全中仍记 0/18**：这一维要落库的是数，词表只证明"如果拿到数，粒度是对的"。
覆盖度矩阵要的是可用数据，不是设计意图——所以粒度那一半进 message 与 sample，
`diseases_covered` 留 0。

**要走通只剩注册 IHME 免费非商用账号这一条**，代价记三笔：license 是 CC BY-NC 4.0，
`commercial_use=False`；`source.auth` 从 none 变 register（登记表建表以来这一列第一次真被用上）；
token 换发叠加 Cloudflare，无人值守采集的失败模式会变多。这笔账 B7c 已判：开——
`auth="register"` 已落进登记表，两行状态记 `paused` 等凭据，注册入口实测定位见《B7c 收口》。
裁定的依据照这一节记着：统计层有 SEER
（美国口径）打底，中国口径那一半随后由 GCO 的两条匿名入口补上（国家级单点估计 + 登记处
年龄别逐年，见下一节），所以注册 IHME 换来的增量已经缩到"1990 年至今的完整年度序列与
1 岁一档"，不再是唯一的中国数来源——开账号买到的是完整年度序列与 CRA 效应量那两半，
不是"补上中国口径的唯一洞"。

复现（离线重放同一份归档会给出逐字相同的 118 个文件行与 6.3 GiB）：

```powershell
ops\etl.ps1 probe --code gbd_results
```

### GCO：真实入口没有门，但门户下是两个数据库

`gco.iarc.who.int/today/` 返回 200 的是 6 KB 的壳，数据在
`https://gco-api.iarc.fr/api/globocan/v3/<版本>/` 与
`https://gco-api.iarc.fr/api/overtime/v2/<版本>/` 两个 JSON API 上，
**全部匿名直连可取，没有 GBD 那道登录门**——可达性实测 direct/200，`source.auth` 仍是 none。

版本号不是常量也不是 URL 的一部分：`v3/[vdb]` 里的 `[vdb]` 是前端构建常量
（Today 的 bundle 里写着字面量 `data_version` 后跟一个四位年份，Over Time 的
`chunk-vendors.js` 里写着 `api/overtime/v2/22/`），两个探针都从 bundle 正则读，
换季版时不用改代码。

**一个门户两个数据库**，而且癌种码空间互不通用：

| | Cancer Today | Cancer Over Time |
|---|---|---|
| 是什么 | GLOBOCAN 国家级估算，一版一个年份 | 登记处汇编的逐年观测序列 |
| 中国 | country=156 | 同，但 `bool_national=False` |
| 年龄组 | **没有**（路径末段实测是癌种过滤器，`ages_specific=1` 无效） | 18 个 5 岁档 0-4…85+，另有 `unk` |
| 年度 | 单点（公开发布共 7 档：2012/2018/2020/2022 1.0·1.1·1.2/2024） | 2002–2017 共 16 年 |
| 肺 / 结直肠 / NHL / 骨髓瘤 | 15 / 41 / 34 / 35 | 11 / 106 / 26 / 27 |
| 中国发病 | 18/18 | 18/18 |
| 中国死亡 | 18/18 | **0/18**（`mortality=false`，`type=1` 实测回 0 行） |

`C88` 在 Today 归 Non-Hodgkin lymphoma(34)，在 Over Time 归 Multiple myeloma(27=「C88+C90」)，
所以映射不能共用一列，`targets.py` 里声明成 `gco_today` 与 `gco_time` 两列，两个探针各核各的。

**Today 达标 18/18**（`verdict=ok`）：一次 `factsheet/population/156/` 回 279 行 =
sex 0/1/2 × type 0 新发/1 死亡/2 现患 × 34 个癌种码，每行带 `total / asr`（世界标化）
`/ crude_rate / cum_risk_74`，另附 `meta/cancers`(41)、`meta/populations`(238 地点，
带 `method_incidence=2b` 这类估算方法码) 与 `meta/update`。三个坑：现患一个
(sex,cancer) 有 3 行而不带期间标签（1/3/5 年混在一起，得回 `description` 读口径）；
`meta/update` 的 14 条里有 6 条 `intern=1` 的内部预发布（`ps`、`2024dev`、`2024ssa`），
不能当数据集登记；跨版本拼出来的"序列"每步都换了估算口径，不能当趋势线画。

**Over Time 达标 18/18 发病、0/18 死亡**（`verdict=partial`）：一次
`data/rate/0/0_1_2/156/all/` 回 1248 行 = 16 个年度 × sex 0/1/2 × 28 个癌种码，每行的
`ages`（18 档计数 + `unk`）/ `populations`（同构分母）/ `age_specific_rate`（同构率）
三个字典一起给，另附 `asr / asr_e / asr_e2013 / asr_n / cum_risk_74 / cum_risk_79`。
**这就是 SEER 只有 8 档宽组、GBD 数值全在门后之后，唯一匿名拿到 ≥5 年 × ≥10 年龄组的一条路。**
两条限定随数值一起落库：中国 `bool_national=False`、`inc_cov=0.6`、`inc_source` 明写
Shanghai / Jiashan / Zhongshan / Nangang(Harbin) 等 5 个登记处——是登记格外推而不是全国序列；
且这一路中国只有发病，死亡年龄段仍空。

复现：

```powershell
ops\etl.ps1 probe --code globocan
ops\etl.ps1 probe --code gco_overtime
```

### WHO GHO：形状全中，国家级与年龄组从不同时出现在同一行上

统计层死亡那一半的备选取数路径，判据是中国国家级 × ≥10 年龄组 × 癌种死亡数。
**实测 `verdict=empty`，覆盖 0/18**，缺口没被填上。

计划里写的 Athena 入口（`apps.who.int/gho/athena/api/…`）已整条 302 到
`www.who.int/data/gho/legacy`，后继是 `https://ghoapi.azureedge.net/api` 的 OData：
匿名、无 key、每个指标一个实体集，`$metadata` 12 MB 所以取数只能点名，
网关把 `$top` 硬限在 1000（超了回 400，原因只写在响应体里）。
可达性分两档：`$top=5` 的单指标小查询 direct/200，逐指标翻页的整趟探针有一步落代理——
`scheduler` 不能照 `probe-reach` 的结论给专项探针配直连。

维表这一半是齐的，缺的全在国家级行那一半：

| | 实测 |
|---|---|
| 维度形状 | `Dim1/Dim2/Dim3` 带 SEX、AGEGROUP、GHECAUSES，列序逐指标不同，须按 `DimNType` 认领 |
| 年龄组 | `AGEGROUP` 维 17 档成体系的 5 岁阶梯（`YEARS00-04`…`YEARS80-84` + `85PLUS`） |
| 死因 | `GHECAUSES` 159 档，`GHE061–GHE079` 共 19 个癌种档 |
| 带死因维的指标 | 22 个，其中 **14 个**有 `SpatialDim eq 'CHN'` 行 |
| 这 14 个里有年龄维的 | **0 个**——只有 SEX 与死因两维 |
| 形状最合判据的 8 个（`GHE_DALY*`/`YLL*`/`YLD*`、`MORT_600/700`） | `SpatialDimType` 只有 `MGHEREG`，**CHN 零行**，只有区域与收入组聚合 |
| 中国癌种能到的最细粒度 | `GHE061`「Malignant neoplasms」一档合计（`SDG_SH_DTH_RNCOM` 240 行 = 2000–2019 × 3 性别 × 4 个大组，无年龄） |

还有一条与取数无关的上限：`GHECAUSES` 本身给不出器官级——肾/脑/甲状腺合并在 `GHE078`
「Other malignant neoplasms」，NHL 与骨髓瘤共用 `GHE076`，18 病只有 13 病能一对一。
即使将来放出国家级 × 年龄组，这一路也只到 **13/18**。

`upstream_version` 取归档行里 `Date` 的最大值（本次 `gho-2025-03-19`），
`release_date` 留空：行级 `Date` 是修改戳，不是发布戳。

复现：

```powershell
ops\etl.ps1 probe --code who_gho
```

## B4 实测结论：危险度（GBD CRA 这一半）

判据是每病 ≥3 个危险因素**带 PAF 或 OR + CI**。实测 `verdict=blocked`、**效应量 0/18**；
同一趟量到的关联骨架是 **10/18**——关联与效应量这两件事必须分开报，混在一起就成了
"GBD 危险因素可用 / 不可用"这种没用的话。GWAS Catalog 那一半见下一节，结论正好相反。

授权面逐个复核，结论与 gbd_results 同一道门但入口不同：

| vizhub GBD Compare 的面 | 实测 |
|---|---|
| `GET /api/config` | 200 匿名，内容只有版本面（`releaseText=GBD 2023`、`gbdYear=2023`、`copyYear=2025`） |
| `GET /api/metadata`、`/api/data`、`/api/hierarchy`、`/api/data/version` | 一律 **401** `Unauthorized.` |
| `GET /api/language`（对照） | 404 `Not found.` —— 所以那四个 401 是"路由在、要授权"，不是整站兜底 |

数值要走通只能注册 IHME 免费非商用账号（Azure AD B2C，scope `data-api/data.read`）。

匿名可取回的是 data guide 页挂着的 `IHME_GBD_2023_A2_RESULTS_BY_MEASURE_Y2025M10D23.XLSX`
（294 KB / 5 张表）：

| | 实测 |
|---|---|
| `Risk` 表 | 真数据 **2390 行** = 2390 个 cause×REI 对（217 个病因 × 88 个 REI）。openpyxl 的 `read_only` 报 8917 行，其中 6527 行整行空白——行数只按带真值的行登记，否则 `dataset_release` 会记下这份文件没有的规模 |
| 度量列 | `Deaths/YLLs/YLDs/DALYs` 四列的值只有 `X`（该组合有数）或空，**数值型列 0 个** → 能建关联、建不了 `paf` |
| `Cause` 表 | 381 档，`targets.py` 声明的 `gbd_cause` **18/18 在列**（这份表同时可作 gbd_results 词表的同源替代） |
| REI 层级 | A2 里没有 parent/level 列，2023 版层级 record 页面上没有任何未登录文件链接 → 只能继续用 2021 codebook 的 `REI Hierarchy`。本版 88 个 REI 全部在 2021 表里找得到，两版之间没出现过重编号 |

聚合档必须剔，否则一个危险因素会被数成十几个（`Dietary` 与它下面十几个子档会同时出现在同一病的行里）。
规则是**"本病集合内仍有后代的不计"**，不是 `level≥3`——`High body-mass index`、
`High fasting plasma glucose` 是二档却自带暴露与 PAF，按层数切会把达标病数从 10/18 误判成 7/18。

去重后每病的独立危险因素数：

| | 独立档数 | 达标（≥3） |
|---|---|---|
| lung、colorectum、breast_female、liver、pancreas、esophagus、prostate、leukemia、stomach、kidney | 16 / 11 / 7 / 5 / 4 / 4 / 4 / 4 / 3 / 3 | ✅ 10/18 |
| cervix、ovary、bladder、thyroid、uterus、nhl、myeloma | 2 / 2 / 2 / 1 / 1 / 1 / 1 | ❌ |
| brain | Risk 表里一行都没有 | ❌ |

度量标记不影响这个结论：这 10 个病的独立档**每一个**都带 Deaths 标记，且 2390 个对里
`YLDs` 有数而 `Deaths` 没数的一个都没有——只取"死亡这一半能落库"的危险因素，达标病数不变。
落库含义：`disease_risk_factor` 的关联与 `role` 能建，`paf` 与置信区间留空。

版本戳取自文件名（`2025-10-23`），`release_date` 一并填它——这是发布日而非修改戳，与 who_gho 相反。
`download_url` 登记的是挂附件的那页而不是附件 URL：附件名带发布日，下一个 GBD 版本一发出来
`probe-reach` 就会报 404，看着像源没了；A2 是否还在由探针判定（链接消失即 `dead`）。

复现：

```powershell
ops\etl.ps1 probe --code gbd_cra
```

## B4 实测结论：危险度（GWAS Catalog 这一半）

同一道判据，跑的是匿名整包 `releases/2026/09/` 的 `…ontology-annotated-full.zip`
（2026-09-04 发布，73,489,749 B；里面那张关联表展开 740,716,183 B、1,192,032 行 / 38 列）。
实测 `verdict=partial`、按**主条目**覆盖 **14/18**；P1 的 C1a 把掉队四病裁成
`targets.GWAS_URI` 声明后，按声明档覆盖 18/18（口径见本节末）。

这一半与 CRA 恰好互补，两半合起来才是危险度维的真实形状：

| | GBD CRA（A2 交叉表） | GWAS Catalog（全量关联） |
|---|---|---|
| 关联（病 × 因素） | ✅ 2390 个 cause×REI 对，骨架 10/18 | ✅ 主条目有行 16/18 |
| 效应量 | ❌ 度量列只有 `X` 标记 | ✅ `OR or BETA` 非空 84.3%，`95% CI (TEXT)` 含可解析区间 75.0% |
| 因素语义 | 可干预暴露（吸烟、BMI…） | 遗传位点，不是可干预暴露 |
| PAF | 无 | 无（全表 38 列没有一个归因分数列） |

四条口径，都会直接决定 P1 能不能自动落库：

1. **CI 非空 ≠ 有区间**。`95% CI (TEXT)` 非空 991,389 行（83.2%），其中 97,360 行（8.2%）
   根本没有区间，只有 `unit increase`(25,793)、`unit decrease`(24,474)、
   `z score increase`(9,888)、`[NR]`(5,005) 这类文字；真含 `[a-b]` 区间的是 894,029 行
   = 全表 75.0%。那 97,360 行里含 2 个畸形串（`[1.80-.5.00]`）按解析失败计——
   宁可不计数也不猜边界。所以"CI 填充率"只能按含可解析区间报。
2. **`OR or BETA` 是一列混装，符号不在这列里**。全表值 <0 的只有 6 行，
   方向写在 CI 文本的 `unit increase/decrease` 注记里——带这条注记的有 841,694 行，
   与含区间那批有重叠（主流形态就是 `[1.09-1.22] unit increase`，两个计数同时命中这一行），
   所以这两个数不能相加。
   OR 与 β 在区间同为正时形态分不开，`effect_kind` 只能整列留未判定，
   不许按"值 <1 就是保护性 OR"这种猜法。
3. **疾病侧以 MONDO 为键**。`MAPPED_TRAIT_URI` 的值就是
   `purl.obolibrary.org/obo/MONDO_0008903` 这种形态（24619 个 trait 档里 2129 个是 MONDO_），
   所以 B2 那个"EFO xref 只有 5/18"的缺口在这里不构成障碍——方向反过来，
   拿 targets 的 `mondo_id` 精确命中即可，不猜名字。
4. **`MAPPED_TRAIT_URI` 是多值列，分隔符实测是逗号不是分号**。96,520 行一行带 2~7 个档，
   因为 MR 研究把暴露档与疾病档并在同一行（`diet measurement, colorectal cancer`）。
   整格不拆开就取尾段，这 1,339 行会伪装成"这一病自己的一条遗传证据"，
   把膳食暴露的遗传工具变量当成结直肠癌的位点证据——16/18 与 14/18 两个数都会虚高。
   所以探针只把单档行算进命中，混档行按病进 `sample.multi_uri_rows`
   （结直肠 798、肺 116、卵巢 78、骨髓瘤 69…）。同一行里 `MAPPED_TRAIT` 与 URI 共用逗号、
   但两列拆出来的档数对不上的有 9,191 行（有的档名自己就带逗号），
   所以拆档只认 URI，label 只能当整串文本用。

覆盖度卡在**粒度**不在字段。按 targets 声明的主条目 URI、要求 ≥3 个带效应量与区间的独立位点，
只到 14/18，掉队的四病是：

| 病 | 主条目行 / 带效应量与区间的位点 | 同级候选档（行数） |
|---|---|---|
| breast_female | 0 / 0 | breast carcinoma 1832、breast cancer 319、ER-negative 84 |
| uterus | 0 / 0 | endometrial carcinoma 102、endometrial cancer 28、uterine carcinoma 5 |
| pancreas | 12 / 0 | exocrine pancreatic carcinoma 171、pancreatic IPMN 16 |
| esophagus | 7 / 1 | esophageal squamous cell carcinoma 50、carcinoma of esophagus 30、esophageal adenocarcinoma 25 |

把同级候选档并进来 18/18 都过 ≥3 位点，但那是**上界**：P0 阶段 `diseases_covered` 记 14/18，
并档必须变成 targets 里逐病声明的 URI 列表（人工裁定），不能让归一化脚本按关键词并。
候选档里也确实混着必须踢掉的东西——`family history of breast cancer`（那是暴露不是结局）、
`breast benign neoplasm` 与 `uterine benign neoplasm`（良性）、`ovarian endometrioid adenocarcinoma`
（挂在子宫体候选档里，属跨病）。探针把逐病候选档连行数一起放进 `sample`，就是为了让这次
裁定不用再回源站。

**C1a 已把这次裁定落成 `targets.GWAS_URI`**——形状与 `OT_NODE` 一致：一份源级例外声明，
不动 `mondo_id`（动了会同时改坏 mondo 与 icd10 两处对齐）。四病各认领哪几档、为什么排掉
分子亚型与癌前档、乳腺癌那一档不分性别的代价，逐条写在那份声明的注释里。探针从此分三个
口径报，覆盖度矩阵那一列读第 ② 个：

| 口径 | 命中依据 | 本次 | sample 字段 |
|---|---|---|---|
| ① 严格 | 只有 `mondo_id` 主条目 | 14/18 | `pass_main` |
| ② 落库 | 主条目 + `GWAS_URI` 声明档 | **18/18** | `pass_declared`（`diseases_covered` 记这个） |
| ③ 上界 | 再加没声明的关键词候选档 | 18/18 | `pass_all` |

①不因为换了声明而消失——主条目自己有没有数，仍然是库里看得见的证据。③在 ② 已经达线时
仍然报出来，它的意思是"还有没裁进来的档"，不是"覆盖还能更高"。要再放宽只能是改那份声明。

落库含义：GWAS 只喂 `role='genetic'` 那一层，每条带 `OR/β` 与区间，`paf` 留 NULL、
`effect_kind` 留未判定。声明档带进来的位点与主条目带进来的同权落这一层，
逐病 label 分布在 `sample.declared_labels` 里可对账。它补不上 CRA 缺的"可干预暴露 + 归因强度"，
所以危险度维在匿名侧仍然是**有清单、有遗传效应量、无归因强度**。

取数姿势：整包必须按 8 MB 分段 Range 续传。单条长连接实测在 53,767,694 B 处被断流，
而且以 HTTP 200 正常收尾——只看状态码会拿到一个残包，`zipfile` 报"不是 zip 文件"，
看着像源坏了。前缀采样也不行：关联表按录入年份排序，取前若干行会把填充率系统性量偏。

复现：

```powershell
ops\etl.ps1 probe --code gwas_catalog
ops\etl.ps1 probe --code gwas_catalog --offline   # 重放同一份归档，9 s
```

## B5 实测结论：研究层

判据那一行是三个源合起来判的：`每病在注册试验 >0、近 5 年文献 >0、OpenTargets 关联 ≥10`。
三列都 18/18，所以**前沿研究这一维进 MVP，不走"从 MVP 剔除"那条退路**。
最低的三个数离判据线都很远：在招试验最少 thyroid 292 项，近 5 年文献最少 uterus 10,366 篇，
关联靶点最少 breast_female 643 个（判据是 10）。

三个源给了三种疾病键，这是这一批真正的产出：

| | 疾病键 | 匿名可取回 | 匿名取不回 |
|---|---|---|---|
| ClinicalTrials v2 | 只能按声明查询词——`fields=` 白名单里没有可对齐的 MeSH ID | 命中数、状态分层、23 列协议字段 | 地理与性别/分期筛选 |
| Europe PMC | 同上——`MH:` 字段只覆盖全库 2.2%，当不了键 | 命中数、OA/IN_EPMC facet、记录级 flag、core 字段 | 数据发布日（只给搜索版本 6.9） |
| Open Targets | **MONDO ID 直接命中**（冒号换下划线） | 带分数的靶点清单、逐数据源分、在研药数 | 按名字换更宽的档——要人裁 |

四条口径会直接决定 P1 怎么写抓取脚本：

1. **CT 的引号会换检索模式**。裸写走 MeSH 概念映射，加引号走字面短语。同一病按同口径比，
   裸写主题词比整串加引号多 9,787 项（18 病合计），而声明的自由文本同义词又在主题词之上
   多并进来 2,823 项、13 病有增量（brain +1,021、liver +606）——**两个增益方向不同，省掉任一都数错**。
   但同义词绝不能裸写：8 个词裸写命中超过加引号的 2 倍（colorectum「colon cancer」8,256 对 2,273、
   cervix「cervical cancer」11,331 对 2,538、breast_female「female breast carcinoma」4,182 对 78），
   因为非主题词裸写退化成词级匹配，会把别的癌种灌进来。所以主查询固定为
   「第一项裸写 OR 其余加引号」，逐词裸/引比值进 `sample` 当词表体检。
   计数有机制：`countTotal=true` 让顶层多一个 `totalCount`，实测与逐页数出的行数完全相等——
   这一支第一版不知道它，按 pageSize=1000 翻页数了约 160 个请求 467 s（那行裁定留在
   `source_probe_log` 里当运行史，dataset_code=`studies-query-cond`，已被取代）。
   在招可以问（`filter.overallStatus` 与 `query.cond` 同时生效），地理不能问
   （`filter.geo` 三种写法与 `aggFilters=geo:CHN` 都拒），所以"有没有中国参与"只能把
   `locations.country` 取回自己数：**900 条抽样里 141 条有中国大陆地点（15.7%）**。
   这一支第一版把它数成 0——不是源没给地点，是取值的路径 walk 不穿数组
   （`locations` 是一组对象），同一次修正让 `location_countries` 从"填充不足五成"回到实到值。
2. **全文可得率是两个数，不是一句"可得率多少"**。facet 口径 OPEN_ACCESS 50.8%、IN_EPMC 56.5%，
   同一批查询的记录级抽样 900 条只有 31.7% `isOpenAccess`。差额是抽样构成造成的
   （抽到的源里 MED 845、PPR 47、PMC 8，而单病实测 MED 内部 OA 只有 24.0%、PMC 内部 81.8%）。
   **B6 判断 L3 能不能拿到正文要看 31.7% 这个数**，前端展示用 50.8%。
   年份过滤必须配 `lucene=true`——`PUB_YEAR:2021-2025` 那种连字符写法被静默忽略，
   回的是不加过滤的全库数，一个错字都不给；查询串一律交给 `urlencode` 生成，
   自己拼 `%20` 会量出不同的命中数（实测 20,275 对 3,474）。
3. **OT 的窄档问题是按病分布的，不是普遍现象**。`literatureOcurrences.count` 在层级上不单调：
   lung 自己 381,600 篇，比它的父节点（3,683 / 511 / 1,636）大两个量级——这类病不用管；
   但 breast_female 只有 88 篇而父节点 breast carcinoma 710,750 篇、pancreas 382 对
   父节点 166,685。**这一维要显示"这一病的研究热度"，宽窄档得逐病重新声明**，
   探针不自己按名字换档（换了就破坏 `icd10` 语义对齐）。B7c 已裁：只把乳腺癌换到
   `MONDO:0007254`（换后 lit 724,160 / drugs 1,036），胰腺不换——它的父节点在研药反而更少
   （30 对本节点 463），换过去研究层退化；这再次说明层级上量不单调，不能整维一刀切。
   声明形状与换档旁的对账取数见《B7c 收口》。
   取数姿势两处：疾病侧点查一次 18 病全中（`MONDO_0008903` 这种下划线形态），
   但 `associatedTargets` 之外一旦带上 `parents` 批量就 HTTP 408，只能 6 病一切；
   靶点清单带 `score` 与 `datasourceScores`（lung 前三：EGFR 0.901、KRAS 0.858、ERBB2 0.842）。
4. **整包不是"顺手就能下"的量级，而站点按点查用就够**。平台 26.06（发布 2026-06-23）的 `output/`
   是 56 个数据集 / 1,102 个 parquet 分片 / 58.5 GiB（最大 colocalisation 20,000 MB）。
   croissant 清单的 56 个 FileSet 目录名与 FTP 实到的 56 个完全一致（对账过），
   但它只给三个分发根、不给单文件大小，`distribution` 根条目里的 `sha256` 字段值是字面量
   `'sha256'` 占位——所以体量只能逐目录列，这一趟 57 个 LIST 请求。
   **`download_url` 回填的是那份 croissant 清单**，不是某个 parquet。

落库形状随之定了：`trial` 逐病按主查询分页取 23 列，`overall_status` 存原值 + 我们的三档映射
（在招 / 未招募但随访 / 已完成），"有没有中国参与"只能落 `locations.country` 数组再前端筛；
`literature` 按 pmid 建键（填充率 93.9%，DOI 98.6% 但只当可选链接），逐篇存 OA 与 IN_EPMC flag
而不是存一个库级率；靶点与药反查走 OT 的点查结果落关联表，`paf` 那类归因分数这里没有。

稀疏列按性质允许为空，不算未达标：23 个候选列实测过半有值的是 18 个，
`collaborators` / `location_countries` / `other_outcome` / `publications` / `why_stopped` 低是试验本身的性质。

给 B6 顺带一条负向证据：**OT 的 `phenotypes.count` 有 9 病是 0**（liver、breast_female、prostate、
thyroid、kidney、brain、uterus、leukemia、nhl），其余病也只有 2~8 条——靶点库的表型注释当不了症状维的源。

还有一件要人裁的：croissant 清单的 `license` 字段写的是 CC0，
而登记表里 OT 那一行写的是"平台 Apache 2.0，数据随上游"——**署名口径以清单为准还是以平台条款为准**，
探针不替它改，两句话现在都在库里。

复现：

```powershell
ops\etl.ps1 probe --code ctgov_v2      # 149 s，249 个请求
ops\etl.ps1 probe --code europepmc     # 183 s
ops\etl.ps1 probe --code opentargets   # GraphQL 点查 + FTP 逐目录列表
ops\etl.ps1 probe --code ctgov_v2 --offline
```

## 症状维的实测结论

这是全站最弱的一环。下表是**结构化本体侧的负向证据**（零抽取能拿到什么），
B6a 跑完后它只说明一件事：症状维不能靠本体，但**不等于要靠模型**——
正文 HTML 里的症状清单是现成的 `<ul><li>`，规则解析就够，见《B6 实测结论》。

| 源 | 实测 |
|---|---|
| HPO `phenotype.hpoa`（v2026-09-02，35.8 MB / 252467 行 / 11598 病） | 第一列零个 MONDO ID；常见癌 3~9 行，且那些"症状"是肿瘤本身（`HP:0003002` Breast carcinoma、`HP:0006753` Neoplasm of the stomach），Frequency 全空。肺癌/胰腺癌/AML 作为条目 0 行 |
| Orphanet `en_product4.xml`（2026-07，45.7 MiB，CC BY 4.0） | 4357 病 / 116664 行，**每行都带六档频率带**（Very frequent 99-80% / Frequent 79-30% / Occasional 29-5% / Very rare <4-1% / Obligate / Excluded），可直接映射 `freq_band`。但只覆盖罕见病：肺/结直肠/前列腺/胃/宫颈/成人肝 0 命中；血液肿瘤可用（CML 11 条、滤泡淋巴瘤 17 条） |
| NCIt 26.02d（248 MB obo 全量扫） | `NCIT:C2926`（肺 NSCLC）零条 relationship；`Disease_May_Have_Finding` 在疾病段几乎不出现 → 不可用 |
| Monarch v3 API | 肺腺癌 0 条、胰腺腺癌 0 条；膀胱癌 8 条真可用（血尿/排尿困难/腹痛/高钙血症，带频率）；甲状腺癌 191 条是亚型上卷，精度差（混入"厚下唇""漏斗胸"） |
| HPO 遗留 `common_disease_annot.tar.gz`（2015 文本挖掘） | 原发性肝癌 111 行、结直肠 111、食管 81、胰腺 55，但 16495 条癌症行 Frequency 全空，58 个癌文件只有 1 行 `HP:0002664` Neoplasm；已被 HPO 主线弃用 → 只能当症状词表候选 |
| SymMap / NCI PDQ API | SymMap 域名已无法解析；PDQ 没有 JSON/XML 端点，只有 HTML |
| **NCI PDQ HTML**（B6a，2026-09-08） | **症状维过判据**：18/18 病各取回 ≥4 条症状项、合计 209 条，5 病目测 precision 100%（判据线 ≥80%）。症状小节里百分号出现数为 0，所以 `freq_band` 这一列这一源供不了 |

结论：结构化本体喂不出常见上皮癌的症状，这条路确实堵死。但 B6a 证明兜底不需要 L3——
PDQ 的 HTML 症状清单 L2 规则解析就能逐条取回。剩下的两处也都不是模型能补的东西：
中文症状名（四条匿名公开路实测全不成立，见 B6c 小节）、频率带（PDQ 症状小节一个百分号都没有，
全站只有 Orphanet 那六档而它不覆盖常见癌——抽取变不出源里没有的东西）。

## 三级抽取

| 级 | 含义 | 适用 |
|---|---|---|
| L1 | 码表/统计库直取，零歧义 | ICD-O-3、MONDO、GBD、SEER、GWAS Catalog、OpenTargets、ClinicalTrials、Europe PMC |
| L2 | 规则化 HTML 解析 | SEER Cancer Stat Facts 的统计表、WHO fact sheet、**NCI PDQ 的症状清单**——症状维实测落在这一级 |
| L3 | 一次性 LLM 抽取兜底 | **本轮不启用**。八维实测下来没有一维需要它：症状落在 L2，其余全是 L1 结构化直取。原本唯一还想要它的地方是中文维基的散文症状段（见 B6c），放弃的理由有三条——中文分句 + 症状判定的精度不可控、CC BY-SA 的相同方式共享会连带污染派生表、而那 11 病本来就有 PDQ 的达标清单，补一份中文并列清单不是缺口 |

L3 若以后真要开，落库规范定死在此：必带 `extract_method='llm_extract'`、
`review_status='unreviewed'`、`provenance='derived-from-PDQ-<rev>'`。
自动作业之后只负责检测上游版本变化并重新出草稿，不覆盖已确认行——
判定依据是 `dataset_release.upstream_version`。

**质量怎么测**：症状维实际落在 `etl/onco_etl/probes/nci_pdq_html.py`——18 病逐病取回认领页面，
只写到 `data/exports/symptom_draft_<code>.md`（不入库），每行带 `source_url` + 锚点 + 原文条目 +
页面 lastmod，中文名列一律留空；判定线是每病 ≥4 条、5 病目测 ≥80% 是真症状
（排除肿瘤同义复述、排除遗传综合征体征），目测结果作为声明写回探针模块的 `EYEBALL`，
由它进 `source_probe_log.message`。实测：18/18 病达线、209 条、precision 100%。
不达线的病症状模块留空并显示"暂无可靠来源"，不硬凑。

## B6 实测结论：症状与叙述

**症状维过判据，而且不需要模型。** PDQ 病人版页面的症状就是现成的 `<ul><li>`，
`probes/nci_pdq_html.py` 用规则解析取回：18/18 病各 ≥4 条、合计 209 条清单症状项，
5 病逐条目测 72/72 真症状（lung 22、colorectum 20、prostate 6、brain 13、bladder 11），
precision 100% 对判据线 ≥80%。逐病条数：myeloma 20、brain 13、liver 12、stomach 12、
cervix 11、bladder 11、lung 11+11（NSCLC 与 SCLC 两份文档）、colorectum 10+10、
leukemia 10+5+8+5（四档）、kidney 6+5、breast_female 8、pancreas 7、esophagus 6、
prostate 6、nhl 7、ovary 5、thyroid 5、uterus 5。这一维从此判定为 **L2**。

三条口径是这个结论的一部分，不是脚注：

1. **入口只能逐病声明**。真入口是 sitemap（`sitemaps/pageinstructions.xml`，6,480 个 loc、
   其中 `/types/` 791 个），摘要页形状 `/types/<段>/<hp|patient>/<slug>-pdq`，
   18 段里只有 12 段开了 `patient/`，另 6 段的病人内容在 `/types/<段>/symptoms` 栏目页
   （这种页全站只有 12 段有，lung/ovarian/thyroid/prostate/pancreatic 都没有）。
   不存在的路径会 301 到栏目页并回 HTTP 200，**返回 200 不等于取到声明的那一页**，所以每页比对
   `final_url`；清单也不总挂在症状标题下（myeloma 两串挂在疾病名节的冒号引言后、
   乳腺全段唯一一份清单挂在男性乳腺癌页），所以取的是"引言句之后紧跟的那一串"。
   代价写在 `targets.py` 的 `pdq_pages`：这份清单只能由人填，30 个页面 18 病逐个认领。
2. **频率带这一列 PDQ 供不了**。症状小节里百分号出现数实测为 0（hp 版整页 1,857 个百分号
   全是生存率与缓解率），11 条含 common/frequent 的条目是症状说法本身（"Frequent urination"）。
   全站只有 Orphanet 那六档能给 `freq_band`，而它常见上皮癌 0 命中——所以这一列按源留空，
   前端不能画成定量结论。
3. **版本戳只能来自 sitemap**。响应头 `Last-Modified` 每页都是同一个站点重建时间，
   PDQ 摘要页页内也没有 Updated，只取 sitemap 的 per-page lastmod（本次最大 2026-07-09）。

**叙述维（WHO fact sheets）不达判据，但它补上了中文症状名这一格。**
判据线是 ≥12/18 病有癌种专页，实测 **4/18**（lung、colorectum、breast_female、cervix），
肝/胃/胰腺/食管/前列腺/卵巢/甲状腺/膀胱/肾/脑/子宫体/白血病/NHL/骨髓瘤 14 病 WHO 没有专页，
这不是抓取失败。另两份通页（Cancer、Childhood cancer）不分病种，能给 18 病共用的概述。
它的独特价值是中文版：真翻译、症状带现成 `<li>`（breast 5 / colorectal 6 / lung 7 条，
目测 precision 100%），所以"中文症状名"这一列有公开、非模型的路——代价是只覆盖 3 病。
其余口径：中文 URL 是换根不是加后缀（`/zh/...` 可取、`/detail/<slug>/zh/` 回 404，
实测 6 份 sheet 六语种全 200）；版本戳逐 sheet 自己更新（认领到的 6 份跨度
2026-02-13…2026-07-03，不能取"今天"）；中英两版的小节集合不是同一套——乳腺癌那 5 条症状
英文版整页没有 Symptoms 节（挂在 "Who is at risk?" 之下），Cancer 通页英文版连 Key facts
一节都没有，所以定位必须逐语种各切一遍；`hasHealthAspect` 只有部分中文页有，只当佐证。

### B6c：中文症状名四条匿名公开路都实测了，没有一条成立

症状清单本身已经是中文的只有 WHO 那 3 病。剩下 15 病要么把 PDQ 的英文翻过来，
要么另找一个中文源。四条匿名公开路各自的实测数（判据线沿用症状维那一行的 precision ≥80%，
下面全是 `probes/wikidata.py` 跑出来的，2026-09-08 一趟 live + 一趟离线重放同数；
第五行是原打算用来兜底、这一轮不启用的 L3，一并列在这免得被当成漏项）：

| 路 | 覆盖 | precision | 为什么不成立 |
|---|---|---|---|
| 拿 PDQ 原文逐条查 Wikidata | 天花板 **21.3%** | — | 169 个去重写法里 ≤2 词仅 36 个、≥5 词 91 个（53.8%），而 Wikidata 查的是**词条**不是短语翻译。36 个短词样本（已是"最顺利的情况"）命中实体 34、带 zh label 30，`anemia` 命中的还是 Q3353348「密穗蕨科」 |
| Wikidata 症状子树当英中词典 | 表面 78.9%（168/213） | **37.5%** | 一次 SPARQL 取回 `wdt:P279*` 子树 10,045 实体、其中 2,158 带 zh label（CC0 许可、无需人工）。按英文 label + alias 子串匹配能落 168 条，逐条目测（去重 135 个配对全判过，未判 0）是 **对 63 / 泛 43 / 错 62** |
| 中文维基条目自己的症状章节 | **5/18** 病有 ≥4 条真症状 | 中文本身全对 | 白血病 10、子宫体 9、骨髓瘤 8、胰腺 5、结直肠 4——但解析条数虚高（结直肠解析 17 条里 13 条是分期定义、骨髓瘤那节标题本身叫「症狀及併發症」）。另 11 病**有章节却写成散文**、2 病连章节都没有，且它是**另一份来源的另一份清单**，不是 PDQ 那 213 条的中文名 |
| 手写英文症状词表 + 子串匹配 | 45.1% | 高 | 词表里的中文是人打进去的，只是把录入从表单搬进了配置文件——撞第一条红线 |
| 中文维基散文段上 L3 抽取 | 11/18 病有散文可挖 | 未测 | 不是"没试"，是**这一轮不启用**（见《三级抽取》）：中文分句 + 症状判定精度不可控，CC BY-SA 同条款共享会连带污染派生表，而那 11 病本来就有 PDQ 的达标英文清单 |

并集才等于"有现成中文症状清单的病"：**WHO 中文版 3 病 ∪ 维基 5 病 = 7/18**，
判据线是 12/18——差 5 病，而且这 7 病里 WHO 那 3 病还带禁商用。

第二条那个 78.9% 是假覆盖率，错法值得记下来：**Wikidata 的 label/alias 是实体名而不是翻译记忆**，
拿它去子串匹配临床散文，命中的永远是含泛词的最长别名实体。
`loss of appetite → 耳咽管開放症`（×8）、`weight loss → 减肥`（×14，取的是"节食"义项）、
`jaundice → 弥散性血管内凝血`、`a bloated feeling after eating → 瘤胃臌氣`（牛的反刍气胀）。
"泛"那一类更隐蔽：所有 `*pain` 都落成「疼痛」、所有 `swelling` 都落成「水肿」，症状身份直接丢掉。
它错出来的中文在 UI 上看着完全正常，这是最坏的一种失败。第一条同源：
36 个短词样本里 `anemia → Q3353348 → 密穗蕨科`（一个蕨类科）、`fever` 命中的 Qid 干脆没有中文标签，
而且 `languages=zh` 取回的是繁简混排（`頭暈` 与 `黄疸` 同屏）。

**裁定：中文症状名不做成翻译列。** `symptom` 落库形状定为
`(disease_id, source_id, name_lang, name)`——PDQ 行 `name_lang='en'`、WHO 中文版 3 病与
中文维基条目 5 病的行 `name_lang='zh'`，两者是**两份不同来源的观察**，
压成同一行的 `name` + `name_zh` 会假装它们是同一份。
前端展示 PDQ 原文并标「NCI 原文」，中文缺口按源如实进覆盖度矩阵。
唯一还没探过的候选是 **CMeSH 中文医学主题词表**（IMICAMS 维护、NLM 有分发），
它是少数可能给出正确英中医学名词对照的公开词表——可达性与许可未测，若以后要补中文名列，从这里开。

剩下那一件是授权边界，B7c 已裁：**站点先按非商用做**。真要商用，出局清单比当时想的短——
叙述维本来就不进 MVP（`who_factsheet` 判据不达线），WHO 那一半只带走 3 病的中文症状清单
（breast_female / colorectum / lung），症状维主体（PDQ 英文 18/18 加上中文维基那 5 病 CC BY-SA）不受影响。

**这一节的数现在可复现**：B7a 已把它落成 `etl/onco_etl/probes/wikidata.py`，
判定进 `source_probe_log`（`dataset_code='zh-label-symptom'`），覆盖度矩阵因此看得见
"中文症状名无源"这一列。落成时改掉了两处一次性脚本带不下来的东西：

1. **标题解析漏了两种边**。`_wiki_titles()` 原本只按 `normalized` 把响应里的页面标题对回病种，
   而简体写法到真页面之间还走着 `converted`（`converttitles` 做的繁简变体）与
   `redirects`（条目改名）两种边。缺了它们，`宫颈癌/甲状腺癌/肾癌/多发性骨髓瘤` 四病
   落回原写法去 `action=parse`，稳定 `missingtitle`，于是被记成"这病没有症状章节"。
   补全后路线③从 3/18 变 5/18、并集从 5/18 变 7/18——**结论方向没变（仍不达 12/18），
   但"哪几病没有"这件事原来报错了四个**，而这正是负结论进库的价值：脚本里没人会去复核的数，
   一旦要按时序重跑、要进矩阵，就会自己冒出来。
2. **解析条数不等于症状条数**。规则解析只能数 `<li>`/`<dd>`，分不清症状项与挂在同一
   名目下的分期定义（结直肠解析 17 条、真症状 4 条），也分不清并发症（骨髓瘤那节标题
   就是「症狀及併發症」）。达标判定因此按声明的目测表 `WIKI_EYEBALL` 走，
   与 `nci_pdq_html` 的 `EYEBALL` 同一口径——人读过的结论是签进代码的配置，不是解析产物，
   也没有任何录入界面。

复现：

```powershell
ops\etl.ps1 probe --code nci_pdq_html      # 343 s，30 个页面 + 1 个 sitemap
ops\etl.ps1 probe --code nci_pdq_html --offline   # 重放归档，不联网
ops\etl.ps1 probe --code who_factsheet     # 77 s，1 个列表页 + 12 页正文 + 36 个语种探测
ops\etl.ps1 probe --code wikidata          # 926 s，1 发 SPARQL + 36 发逐条查 + 18 条目抓取
ops\etl.ps1 probe --code wikidata --offline      # 重放 data/raw/wikidata/zh-*，0.1 s 同数
ops\etl.ps1 matrix                         # 由 source_probe_log 生成 docs/数据源覆盖度.md
```

`wikidata` 排在 `nci_pdq_html` 后面注册：它的分母是 PDQ 归档的症状条目，全量扫之前必须先有那份归档。

## B7b：覆盖度矩阵从库里生成，不再手抄

`etl/onco_etl/matrix.py` 只读 `source_probe_log` 里每个 (源, 数据集) 最近一次专项探针行，
按 `COLUMNS` 声明的取格规则生成 `docs/数据源覆盖度.md`。三条规矩写在模块顶部：

1. **没有逐病布尔就不出格**。聚合覆盖率不许反推逐病格——`gbd_cra` 自报 `diseases_covered=0`
   而逐病能数到 10 个危险因素，这类不一致只能靠"不画这一列 + 在没进矩阵一节写明理由"处理，
   不能由生成器猜。
2. **判据原话从日志取**，列说明那一栏引用的就是 `log.criteria`，不在文档里另写一遍口径。
3. **每列自报数与逐病格数对账**，对不上就在《对账》一节点名，不静默。

当前 13 列 × 16 份裁定，对账无差异；`histology` 一维没有逐病明细可画，
在《没进矩阵》一节里如实标为"全局码表、无逐病格"。

## B7c 收口：五项悬置与 IHME 授权面实测

十三维逐维进不进、缺的那半在页面上怎么显示，写在 `docs/MVP裁定.md`，这一节不重复。
这里只留两件与探针计划直接相关的实测：卡在门后的那两维到底被什么协议挡着，
以及 B5 攒下来的窄档裁定落成了什么形状。

**IHME 授权面**（2026-09-08 直连实测，不是文档转述）。"找不到注册入口"不是入口藏得深，
是**它就没有独立注册页**——`id.healthdata.org` 这个域名在本机根本不解析。最短的一条路是
`https://vizhub.healthdata.org/gbd-results/` **一打开就自动弹**的注册对话框（卡上"注册 / 登录"
两个按钮），顶栏 Account 菜单里的 Register 是同一件事的第二条路（窄屏下整个顶栏收进汉堡按钮，
菜单不展开就看不到）。注册与登录共用同一条 Azure AD B2C 流，拆 vizhub 前端配置拿到的真值：

- 授权服务器 `https://login.healthdata.org/a07655f6-e482-42f3-8b30-6b7d009f813d/B2C_1A_SIGNUP_SIGNIN`
  ——策略名就叫 `SIGNUP_SIGNIN`，一个端点同时管注册和登录；
  `knownAuthorities: ["login.healthdata.org"]`，`redirectUri: "/vizhub/auth-redirect"`，
  clientId `9e66b6a3-5d2e-400f-b812-f60f441d5041`，
  scope `https://ihmecsu.onmicrosoft.com/data-api/data.read`，token 缓存放 localStorage。
  前端里 `show-account="true"` 与 `window.__requireAuth__ ??= true`，
  即整站默认就要登录，与 B3/B4 实测到的"数据面四个路由全 401、只有 `/api/config` 匿名"对得上。
- Register 与 Login 都走 MSAL 的 `loginPopup`，Register 只多带一个 `state={"mode":"signup"}`；
  那条 authorize 页实测可达（PKCE 授权码流回 200），页面配置里
  `showSignupLink=True`、文案 "Don't have an account? **Sign up now**"，
  表单模板由 IHME 自己的 `azure-aad-b2c-templates.ihme.services/SignIn.html` 提供。
- 三条走不通的路记下来省得再试：不带 `code_challenge` → `AADB2C99059`；
  用 v1 端点 `.../oauth2/authorize` → `AADB2C90012`（这个 scope 只认 v2）；
  用 implicit（`response_type=token`）→ `AADB2C90057`（应用未开隐式流）。
  手工拼 authorize 链接能打开注册卡，但换不到 token——`code_verifier` 只在应用的 MSAL 缓存里，
  所以只能走站内那条路，别收藏手工链接。
- 商用边界：注册即可检索下载，但 <https://www.healthdata.org/account-support> 写明
  商用不在非商用条款内，要走 IHME Client Services 谈许可。

探针不接管注册（人机验证 + 凭据不进仓库），所以 `gbd_results` / `gbd_cra` 的状态是 `paused`
而不是 `rejected`，`.env.example` 已加 `IHME_USER` / `IHME_PASS` 占位；
账号一到位重跑这两支，判据里"死亡年龄组"与"PAF"那两半一起补。

**OT 窄档** 落成 `targets.OT_NODE` 一份声明 + `ot_node()` 一个解析函数（不动 `mondo_id`——
改了那一列会同时把 mondo 与 gwas_catalog 两支探针与 icd10 对齐的口径改坏）。
只换乳腺癌：`MONDO:0004379`（assoc 643 / lit 88 / drugs 0）→ `MONDO:0007254`
（assoc 17,064 / lit 724,160 / drugs 1,036）。胰腺不换，因为它的父节点更空
（在研药 30 vs 本节点 463）——关联量在 MONDO 层级上**不单调**，所以不做"整维按父节点"的一刀切。
探针不自己按名字猜档，重跑时对每个被覆盖的病旁查声明主条目并把换前换后一起写进 message，
换档因此是库里可复核的裁定而不是注释。换档后 18/18 `ok`，合计 228,551 条关联，
这一列最少的是 brain 3,868（换档前是 breast_female 643）。

## 候选源清单

可达性一列是 `probe-reach` 的实测结果（2026-09-07，全部 HTTP 200）。
`direct` = 本机直连可取；`proxy` = 直连失败、走 `.env` 里 `PROXY_URL` 才取到。
这个区分是必需的：scheduler 要无人值守跑，不能靠临时代理。

| code | 维 | 形态 | 授权 | 可达性 |
|---|---|---|---|---|
| `mondo` | identity | .obo | CC BY 4.0 | direct 或 proxy（GitHub release 资产，随网络状况） |
| `ncit` | identity, anatomy | .obo | CC BY 4.0，部分术语源自第三方 | direct 或 proxy（同上） |
| `mesh` | identity, literature | XML | 美国政府作品，需署名 NLM | direct |
| `icdo3_seer` | anatomy, histology | xlsx 码表 | 美国政府作品，需署名 SEER | direct |
| `icdo32_naaccr` | histology | xls 码表 | ICD-O-3.2 WHO/IARC，NAACCR 镜像 | direct |
| `wikidata` | identity, symptom, narrative | SPARQL 子树 + MediaWiki API（JSON/HTML） | Wikidata CC0 / 维基正文 CC BY-SA | proxy（**B7a 探针已落**：中文症状名四条匿名公开路全不成立，词典 precision 37.5%、并集 7/18） |
| `orphanet_product4` | symptom | XML | CC BY 4.0 | direct |
| `hpoa` | symptom | TSV | 以文件头声明为准 | proxy |
| `monarch_common_disease` | symptom | tar.gz | 见包内声明 | direct |
| `nci_pdq_html` | symptom, narrative | HTML 正文 + sitemap 供 loc/per-page lastmod（30 页需按 `targets.py` 逐病声明） | 美国政府作品，需署名 NCI | direct（**B6a 已过判据**：症状 18/18、precision 100%） |
| `seer_statfacts` | stat, survival | HTML 表 | 美国政府公开数据 | direct |
| `gbd_results` | stat | 词表 ZIP 匿名 / 数值 CSV·ZIP | CC BY-NC 4.0，**禁商用** | direct（数值要 register） |
| `gbd_cra` | risk | XLSX（A2 交叉表） | CC BY-NC 4.0，**禁商用** | direct（guide 页；探针整趟偶发 proxy） |
| `globocan` | stat | REST JSON | IARC 条款，需署名 | direct（壳已拆，入口 `gco-api.iarc.fr`） |
| `gco_overtime` | stat | REST JSON | IARC 条款，需署名 | direct（同上，另一个数据库） |
| `who_gho` | stat | OData JSON | CC BY-NC-SA 3.0 IGO，**禁商用** | direct（`$top=5` 小查询；逐指标翻页那趟落 proxy） |
| `gwas_catalog` | risk | ZIP 73.5 MB（EBI FTP，件内单张 TSV 展开 740 MB） | CC BY 4.0，需署名 | direct（必须 8 MB 分段 Range；单条长连接在 54 MB 处断流仍回 200） |
| `ctgov_v2` | trial | REST JSON（计数靠 `countTotal=true`，版本戳在另一个路由 `/api/v2/version`） | 美国政府公开数据 | direct |
| `europepmc` | literature | REST JSON（查询串必须 urlencode 生成，年份过滤要 `lucene=true`） | 元数据自由，全文按出版商 | direct（偶发 503，重试即回） |
| `opentargets` | trial, literature | GraphQL 点查 + FTP 整包 56 数据集 / 1,102 分片 / 58.5 GiB | 平台 Apache 2.0，数据随上游（清单自己的 license 写 CC0，待裁） | direct（壳已拆：GraphQL 与 EBI FTP 都是真入口） |
| `who_factsheet` | narrative, symptom | A-Z 列表页供全站 242 份 slug；正文中英两版小节集合不同需各切一遍；六语种换根可得 | CC BY-NC-SA 3.0 IGO，**禁商用** | direct（**B6b 不达判据 4/18**；价值只剩 3 病的中文症状名与两份通页概述） |

壳类源要特别注意：返回 200 的是前端壳，不是数据，真实入口是各自的 API 或 FTP。
现在三个壳全部拆完了，答案两两不同。GCO 的壳背后是两份匿名可取的 JSON API，`download_url` 回填的就是
它们（上面 `globocan` 与 `gco_overtime` 两行，同一门户两个数据库）；GBD 的真实入口存在但排在
注册门后面，所以它的 `download_url` 回填的是那份词表 ZIP，不是数值。`gbd_cra` 回填的是挂 A2
附件的那页 guide（附件名带发布日，不能当入口，详见上节）。OpenTargets 的壳拆出两个真入口，
回填的是 EBI FTP 上那份 `croissant.json` 发布清单——它是唯一一份"一次请求说清这一版有什么"的东西，
56 个数据集与 58.5 GiB 的体量就是从它的 FileSet 逐目录列出来的（详见 B5 那节）。
**填了 `download_url` 不等于这一维通了**，
判据只认探针回来的 `verdict` 与 `diseases_covered`。

`gwas_catalog` 的真实入口是 `ftp.ebi.ac.uk/pub/databases/gwas/releases/<年>/<月>/` 下的
`gwas-catalog-associations_ontology-annotated-full.zip`，`latest/` 只是它的镜像，
所以 `upstream_version` 从日历目录读（实测 `2026-09`）、`release_date` 取这份包在 FTP 上的日期
（实测 2026-09-04），都不拼死。疾病侧对齐靠件内的 `MAPPED_TRAIT_URI`：这一列是混装的（24619 个 trait 档里 `MONDO_` 2129 个，
其余是 `EFO_`、`OBA_` 等），但癌种档正好落在 MONDO 上，所以拿 targets 的 `mondo_id`
直接命中就行，B2 的 EFO 缺口不影响这一路——不许按前缀假定整列同一种 ID。
旧的 `/gwas/api/search/downloads/full` 已 404 下线。

## 批次

| 批次 | 内容 | 估时 |
|---|---|---|
| B1 | 仓库骨架 + `.env` + 四张表 + 迁移器 + 代理与 raw 缓存层 + 可达性探针 | 0.5 d ✅ |
| B2 | ID 主干与器官树（MONDO 交叉引用完整度、ICD-O-3 码表解析、18 病挂载率） | 1 d ✅ |
| B3 | 统计层（SEER 表结构 fixture、GBD 是否有可编程批量入口、GCO 中国口径、WHO GHO 备选） | 1.5 d（SEER ✅ 年龄组差 2 档；GBD ✅ 已排除——数值全在登录门后；GCO ✅ 两个数据库都匿名直连，Cancer Over Time 补上 5 岁组 × 16 年，代价是中国只有发病、且是 5 个登记处覆盖 60% 人口的外推；WHO GHO ✅ 已排除——0/18，国家级与年龄组从不同时出现） |
| B4 | 危险度（GBD CRA 的 cause × risk 交叉、GWAS Catalog ZIP 字段与 CI 填充率） | 1 d（GBD CRA ✅ 效应量 0/18——vizhub 数据面全 401，只能注册账号；关联骨架 10/18 由匿名的 A2 交叉表供，`paf` 列无源；GWAS Catalog ✅ 效应量与 CI 匿名就在整包里（84.3% / 75.0% 含可解析区间）但那是遗传位点、全表无 PAF 列，按主条目 14/18，并同级组织学档是 18/18 的上界；✅ P1 的 C1a 已把掉队四病裁成 `targets.GWAS_URI` 声明，矩阵那一列改读声明档） |
| B5 | 研究层（ClinicalTrials 查询词命中率、Europe PMC 全文可得率、OpenTargets 下载体量） | 1 d（三源 ✅ 18/18，判据那一行三列全过：在招 23,660 项、近 5 年文献 765,891 篇（OA 50.8%）、关联靶点最少 643 个（breast_female 窄档，B7c 换档后这一列最少是 brain 3,868）。CT 的计数有 `countTotal=true`，第一版按翻页数的 467 s 作废；EPMC 的全文率是两个数（facet 50.8% 对记录级 31.7%），L3 看后者；OT 的疾病键就是 MONDO 号，整包 58.5 GiB 不必下、点查够用；breast_female 与 pancreas 落在窄档，B7c 已裁成一份节点覆盖声明 `targets.OT_NODE`——只换乳腺癌，胰腺不换，理由见 B5 那节） |
| B6 | 症状与叙述（PDQ/WHO 页面结构、症状清单抽取档位 + 5 病抽查） | 1.5 d ✅（PDQ ✅ 症状维过判据：18/18 病、209 条、目测 precision 100%，且原定的 L3 兜底被推翻——症状清单是现成 `<li>`，L2 规则解析就够；PDQ 症状小节零频率，`freq_band` 这一源供不了。WHO ✅ 叙述维不达判据 4/18（14 病没有癌种专页，非抓取失败），但中文版给出 3 病现成的中文症状清单、precision 100%。B6c ✅ 中文症状名四条匿名公开路全不成立（Wikidata 子树词典 37.5%、逐条查天花板 21.3%、中文维基 5/18 病有 ≥4 条真症状清单），裁定为不做翻译列） |
| B7 | 覆盖度矩阵自动生成 + MVP 维度裁定 | 0.5 d（B7a ✅ `probes/wikidata.py` 落地，`partial 7/18`、168 行、926 s 走代理；顺带修掉一次性脚本留下的两处错——标题解析漏 `converted`/`redirects` 两种边（曾把 4 病误报成"没有症状章节"），以及拿解析条数当症状条数（结直肠 17 条里 13 条是分期定义），达标改按声明的 `WIKI_EYEBALL` 判。B7b ✅ `matrix.py` + `python -m onco_etl matrix` → `docs/数据源覆盖度.md`，13 列 × 16 份裁定，逐病格与探针自报数对账无差异。B7c ✅ MVP 分维裁定 + 五项悬置收口 → `docs/MVP裁定.md`：逐维裁进/不进，21 个源的 `status` 从 `'candidate'` 翻牌成 12 active / 8 paused / 1 rejected 并已 `seed-sources` 落库，OT 窄档落成 `targets.OT_NODE`（只换乳腺癌），IHME 注册入口实测定位在 GBD Results 页自动弹出的注册对话框（顶栏 Account → Register 是同一条 B2C 流的第二条路，没有独立注册页），`probes/wikidata.py` 的负结论进了矩阵。**P0 出口判据已达成**——覆盖度矩阵 + 分维裁定两份产出都在库里，可以开始建业务表） |

B3 已跑完，结果见上节：四个源没有一个单独够用，组合才补齐统计层与生存率，
而中国死亡年龄组那一半到 B3 收尾仍然空着。B4 两半也都跑完了（见上节）：
CRA 那半有暴露清单、无强度，GWAS 那半有强度、但那不是可干预暴露，
所以危险度在匿名侧只到"有清单、有遗传效应量"为止，归因强度（PAF）仍然空着。
两件事撞在同一道门上——中国死亡年龄组与 CRA 效应量
都只能靠 IHME 注册账号解开，所以这是 B7 的一个决定而不是两个。B7c 已裁定"开"，
入口也实测定位了（见《B7c 收口》），账号到位前两维一起留空。

B5 是目前唯一三列判据全过的一批（见上节），前沿研究这一维因此确定进 MVP。
它留给 B7 的两次裁定都已在 B7c 收口：OT 那两个窄档只有乳腺癌换宽节点（`targets.OT_NODE`），
croissant 的 CC0 与登记表"数据随上游"的署名口径按清单原文记进 `legal_note`、平台条款那侧另记一笔。
（原先还挂着第三条"L3 的正文输入率按记录级 31.7% 算够不够"——B6a 之后症状不吃文献全文，
这个数只在将来真要从文献正文里挖症状时才需要重新拿起来。）

B6 也跑完了（见《B6 实测结论》）：症状维过判据而且落在 L2，原计划里"抽取质量能不能过 80%"
这一项不确定性已经消掉——实测 precision 100%。它没有留下待裁定的缺口：中文症状名那四条
匿名公开路（Wikidata 逐条查、Wikidata 子树词典、中文维基自己的症状章节、手写英文词表）
B6c 都实测过，全不成立，
于是 `symptom` 的落库形状直接定成**按源分行**（`(disease_id, source_id, name_lang, name)`），
不再有 `name_zh` 这一列——翻译列的前提是翻译可信，而实测它不可信。
WHO 禁商用那一条 B7c 裁成"站点先按非商用做"，商用时叙述维与那 3 病中文清单一起重算。

B7 的三项都已落地（见《B7b》《B7c 收口》）：`probes/wikidata.py` 把上面那个负结论变成库里可重跑的裁定，
`matrix.py` 把十三维的裁定汇成 `docs/数据源覆盖度.md`——从此每格的 ✓/✗ 都有 `source_probe_log`
的行号可追，文档不再手抄数；B7c 把这十三维连同四批攒下来的悬置项一次裁完，
产出 `docs/MVP裁定.md`，21 个源的 `source.status` 也从 P0 占位的 `'candidate'` 翻成了
12 active / 8 paused / 1 rejected 并重新 `seed-sources` 落库。

## 三条红线

1. **授权**：`source.legal_note` 为空视为未通过 P0，`seed-sources` 会直接拒绝写入。
   NCCN、UpToDate、Cancer Research UK 条款禁止抓取，不列入候选。
   `gbd_*` 与 `who_factsheet` 是非商用许可，站点若要商用必须先换源。
2. **跨源冲突并存不覆盖**：SEER（美国）与中国国登的 5 年生存率能差三十个点。
   `stat_fact` 与 `survival` 的唯一键都含 `source_id` 与全部口径维度（`dataset_code` 只在
   `stat_fact` 的键里——一张长表要装两套码空间，`survival` 一个源只发一套），
   前端按口径并排展示，禁止任何"取平均"。
3. **`freq` 与 `freq_band` 默认都为空**：B6a 实测症状清单这一路连定性频率都不给
   （PDQ 症状小节百分号出现数 0，含 common/frequent 的 11 条是症状说法本身如
   "Frequent urination"），全站只有 Orphanet 那六档能填 `freq_band`、而它不覆盖常见上皮癌。
   所以这两列按源留空，`freq decimal NULL` 与 `freq_band` 都不许默认值；
   UI 只在真有档时显示区间条，且不用折线——空不能画成"不常见"。

## 常用命令

```powershell
python db/tests/run.py apply db/schema.sql   # 建四张表
python db/tests/run.py migrate               # 应用 db/migrations 下未执行的迁移
python db/tests/run.py status                # 行数 + legal_note 门禁

ops\etl.ps1 seed-sources --dry-run           # 只看候选源，不写库
ops\etl.ps1 seed-sources                     # 登记 21 个候选源
ops\etl.ps1 probe-reach                      # 全源可达性探针
ops\etl.ps1 probe-reach --code mondo         # 只跑指定源
ops\etl.ps1 probe --list                     # 有哪些专项探针
ops\etl.ps1 probe --code icdo3_seer --code mondo   # 跑专项探针：行数、18 病覆盖度、字段
ops\etl.ps1 probe --code mondo --offline     # 离线重放已归档文件，不重新下载
ops\etl.ps1 probe-status                     # 每源每份数据集最近一次裁定
ops\etl.ps1 status                           # 库现状速览
```
